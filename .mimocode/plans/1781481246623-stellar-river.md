# Plan: Final Cleanup

## Overview

All 9 phases are complete. Build passes (58 pages, zero type errors). Only minor cleanup remains.

---

## Cleanup Items

### 1. Remove dead eventService re-export from database.ts
- **File**: `lib/database.ts` line 57 — `export { eventService } from './events'`
- Nobody imports eventService from `@/lib/database` anymore. Remove the dead re-export.

### 2. Consolidate Project type
- **File**: `lib/database.ts` lines 35-57 — has its own `Project` interface and `projectService`
- **File**: `lib/types/index.ts` — also has a `Project` type
- Fix: Remove duplicate `Project` interface from `lib/database.ts`, import from `@/lib/types` instead. Keep `projectService` in `database.ts` since it's the only service left there.

### 3. Clean up database.ts imports
- After removing eventService and duplicate types, `lib/database.ts` will only have: constants, PROJECTS_COLLECTION_ID, projectService. Clean up unused imports.

---

## Files to Modify
1. `lib/database.ts` — remove dead eventService re-export, consolidate Project type

## Verification
1. `npx tsc --noEmit` — type check passes
2. `npm run build` — build succeeds
