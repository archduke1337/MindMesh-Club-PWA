# Plan: Complete All Remaining Features

## Overview

Implement all remaining gaps across event system, dashboards, email, permissions, and profile audit. Use Appwrite Functions for email (not EmailJS).

---

## Phase 1: Unify Event System (Critical — Two Competing Services)

**Problem**: `lib/database.ts` has an old `eventService` + `Event` type. `lib/events.ts` has a new `eventService` + `Event` type from `lib/types/index.ts`. They write to the same Appwrite collection with incompatible schemas. Admin pages use old service; create/edit use new service.

### 1.1 Consolidate to single service
- **File**: `lib/events.ts` — keep as canonical, enhance with missing methods
- **File**: `lib/database.ts` — remove old `eventService` (lines 95-307), keep only `projectService` and `eventService` re-export from `lib/events.ts`
- **File**: `app/events/[id]/page.tsx` — change import from `@/lib/database` to `@/lib/events`
- **File**: `app/admin/events/page.tsx` — change import from `@/lib/database` to `@/lib/events`, update type usage from old `Event` (with `category`) to new `Event` (with `eventTypeId`, `audience`, `status`)
- **File**: `app/events/page.tsx` — change import to `@/lib/events`
- **File**: `app/dashboard/page.tsx` — change import to `@/lib/events`

### 1.2 Add missing event workflow to service
- **File**: `lib/events.ts` — add methods:
  - `submitForReview(eventId)` — status: draft → review
  - `approveEvent(eventId, approverId)` — status: review → approved → published
  - `rejectEvent(eventId, reason)` — status: review → rejected
  - `cancelEvent(eventId)` — status: → cancelled
  - `completeEvent(eventId)` — status: active → completed
  - `autoActivate()` — query events where date <= now and status = published → set active

### 1.3 Fix "Submit for Review" bug
- **File**: `app/events/create/page.tsx` line 44 — `handleSubmit` hardcodes `status: "draft"`. Fix: accept status param, use it instead of hardcoding.
- **File**: `app/events/create/page.tsx` line 108 — "Submit for Review" button should call with `status: "review"`

### 1.4 Add audience enforcement
- **File**: `app/events/page.tsx` — filter events by audience (public events for all, member_only for members+, exclusive for approved registrations only)
- **File**: `app/events/[id]/page.tsx` — check audience before showing register button; for exclusive events, require approval

### 1.5 Wire ticket creation to registration
- **File**: `lib/events.ts` `register()` method — after creating registration, call `ticketService.create()` from `lib/tickets.ts`
- **File**: `lib/tickets.ts` — ensure `create()` uses proper ticketCode and qrData

### 1.6 Wire PDF generation to event page
- **File**: `app/events/[id]/page.tsx` — add "Download Registration List" button for admin/head roles
- **File**: `app/admin/events/page.tsx` — add PDF export button per event

---

## Phase 2: Event Type Schema System

### 2.1 Create event types service
- **File (new)**: `lib/eventTypes.ts` — CRUD for `event_types` collection:
  - `getAll()`, `getById()`, `create()`, `update()`, `delete()`
  - `seedDefaultTypes()` — seed the 8 types from EVENT_TYPES.md
  - `getByEventTypeId(typeId)` — get type config

### 2.2 Create event type data service
- **File (new)**: `lib/eventTypeData.ts` — CRUD for `event_type_data` collection:
  - `getByEventId(eventId)` — get custom field data for an event
  - `save(eventId, eventTypeId, fieldData)` — save custom field values

### 2.3 Dynamic event creation form
- **File**: `app/events/create/page.tsx` — after selecting event type, dynamically render fields from `EventType.fields[]`
- **File**: `app/events/[id]/edit/page.tsx` — same dynamic field rendering

### 2.4 Admin event type management
- **File (new)**: `app/admin/event-types/page.tsx` — view/edit event type schemas

---

## Phase 3: Waitlist & Registration Enhancements

### 3.1 Implement waitlist logic
- **File**: `lib/events.ts` `register()` — when capacity full and `waitlistEnabled`:
  - If not full: create registration with status "confirmed", create ticket
  - If full: create registration with status "waitlisted", no ticket yet
  - On cancellation: promote next waitlisted registration → "confirmed" + create ticket

### 3.2 Registration approval for exclusive events
- **File**: `lib/events.ts` — if event audience = "exclusive", registration status = "pending"
- **File (new)**: `app/admin/events/[id]/registrations/page.tsx` — approve/reject pending registrations

### 3.3 Cancellation deadline
- **File**: `lib/events.ts` `cancelRegistration()` — check if cancellation is within deadline before allowing

---

## Phase 4: Email Service via Appwrite Functions

### 4.1 Create Appwrite Cloud Function for email
- **File (new)**: `functions/send-email/index.ts` — Appwrite Function that:
  - Receives event trigger (member approved, event registration, etc.)
  - Uses `nodemailer` or Appwrite's built-in messaging (if available) to send email
  - Templates: welcome letter, promotion letter, registration confirmation, event notification

### 4.2 Replace EmailJS with Appwrite Function calls
- **File**: `lib/emailService.ts` — rewrite to:
  - Server-side function that calls Appwrite Functions API
  - Or use Appwrite Messaging API if available
  - Methods: `sendWelcomeEmail()`, `sendPromotionEmail()`, `sendRegistrationConfirmation()`, `sendEventNotification()`

### 4.3 Wire email to all notification points
- **File**: `app/api/membership/route.ts` — on approval, call `sendWelcomeEmail()`
- **File**: `app/api/promotions/route.ts` — on promotion, call `sendPromotionEmail()`
- **File**: `lib/events.ts` `register()` — on registration, call `sendRegistrationConfirmation()`
- **File**: `app/api/designations/route.ts` — on designation, call `sendDesignationEmail()`

### 4.4 Update env vars
- **File**: `.env.example` — add `APPWRITE_FUNCTION_ID`, `APPWRITE_API_KEY` for email function
- **File**: `lib/appwrite.ts` — add Functions client if needed

---

## Phase 5: Dashboard Enrichment

### 5.1 Fix hardcoded stats
- **File**: `app/dashboard/page.tsx` — replace hardcoded zeros with real queries:
  - `eventsAttended`: count registrations where userId = current user
  - `activeTickets`: count tickets where status = "issued" or "active"
  - `departments`: count user_departments where userId = current user
  - `badges`: count user_designations where userId = current user

### 5.2 Add "My Events" section
- **File**: `app/dashboard/page.tsx` — add tabbed section:
  - Upcoming: registrations where event.date > now
  - Past: registrations where event.date < now
  - Each shows EventCard with ticket status

### 5.3 Add Announcements section
- **File**: `app/dashboard/page.tsx` — fetch latest notifications, show unread announcements

### 5.4 Add Department Overview for leads/heads
- **File**: `app/dashboard/page.tsx` — for `lead`+ roles, show department stats card

### 5.5 Add Activity Feed for admin
- **File**: `app/dashboard/page.tsx` — for `admin`, show recent audit log entries using `ActivityFeed` component

---

## Phase 6: Profile & Audit System

### 6.1 Enhanced profile page
- **File**: `app/profile/page.tsx` — add:
  - Department display (query user_departments)
  - Designation badges (query user_designations)
  - Membership status display (replace hardcoded "Active Account")
  - Extended field editing: bio, skills, social links, phone
  - Profile completeness progress bar

### 6.2 Wire audit logging to profile edits
- **File**: `app/profile/page.tsx` — on any field change, call `logProfileChange()` from `lib/audit.ts`
- **File**: `lib/audit.ts` — ensure `logProfileChange` is properly exported and works

### 6.3 Profile change history
- **File (new)**: `app/profile/history/page.tsx` — display audit logs filtered to current user's profile changes

---

## Phase 7: Permission & Route Guards

### 7.1 Create Next.js middleware
- **File (new)**: `middleware.ts` (project root) — convert `proxy.ts` logic:
  - Check session cookie for `/admin/*` routes
  - Set security headers
  - Redirect unauthenticated users from protected routes

### 7.2 Add API route protection
- **File**: All API routes under `app/api/` — add session verification at the start of each handler:
  - Check `a_session_*` cookie
  - Verify user status is not banned/deactivated
  - For admin-only routes, verify admin status

### 7.3 Fix blog write permission
- **File**: `app/blog/write/page.tsx` — add `usePermissions` check for `blog_creator` power or `admin` role

### 7.4 Add permission guards to remaining pages
- **File**: `app/admin/users/page.tsx` — add admin check
- **File**: `app/admin/tickets/verify/page.tsx` — add `ticket_verifier` power check
- **File**: `app/admin/gallery/page.tsx` — add `gallery_manager` power check

### 7.5 Role-based navigation
- **File**: `components/navbar.tsx` — add:
  - Notifications bell icon with unread count badge
  - Admin link in user dropdown (only for admin users)
  - "My Events" quick link

---

## Phase 8: Admin Event Enhancements

### 8.1 Event approval pipeline
- **File**: `app/admin/events/page.tsx` — add tabs: Draft | Review | Approved | Published | All
- **File**: `app/admin/events/page.tsx` — add approve/reject buttons for "review" status events

### 8.2 Registration management per event
- **File (new)**: `app/admin/events/[id]/registrations/page.tsx` — list all registrations with:
  - Status filter (confirmed/pending/waitlisted/rejected)
  - Approve/reject pending registrations
  - Export to PDF (wire `lib/pdf.ts`)
  - Bulk actions

---

## Phase 9: Remaining Polish

### 9.1 Onboarding redirect
- **File**: `app/onboarding/page.tsx` — if user already has application, redirect to `/dashboard`

### 9.2 Notification bell in navbar
- **File**: `components/navbar.tsx` — add bell icon, fetch unread count, link to `/notifications`

### 9.3 Seed default event types
- **File (new)**: `lib/seed.ts` — function to seed 8 default event types into `event_types` collection
- Call on first admin login or via API route

---

## Files to Create (New)
1. `lib/eventTypes.ts` — Event type CRUD service
2. `lib/eventTypeData.ts` — Event type data service
3. `middleware.ts` — Next.js middleware (route protection)
4. `functions/send-email/index.ts` — Appwrite email function
5. `app/admin/event-types/page.tsx` — Event type management
6. `app/admin/events/[id]/registrations/page.tsx` — Registration management
7. `app/profile/history/page.tsx` — Profile change history
8. `lib/seed.ts` — Seed script for event types

## Files to Modify (Existing)
1. `lib/events.ts` — Add workflow methods, waitlist, ticket creation
2. `lib/database.ts` — Remove old eventService
3. `lib/emailService.ts` — Replace EmailJS with Appwrite Functions
4. `app/events/[id]/page.tsx` — Fix imports, add audience check, PDF button
5. `app/events/create/page.tsx` — Fix submit bug, add dynamic fields
6. `app/events/[id]/edit/page.tsx` — Add dynamic fields
7. `app/admin/events/page.tsx` — Fix imports, add approval pipeline
8. `app/dashboard/page.tsx` — Fix stats, add My Events/Announcements/Activity
9. `app/profile/page.tsx` — Add extended fields, audit, designations
10. `app/blog/write/page.tsx` — Add permission guard
11. `components/navbar.tsx` — Add notifications bell, role-based nav
12. `app/api/membership/route.ts` — Add email on approval
13. `app/api/promotions/route.ts` — Add email on promotion
14. `app/api/designations/route.ts` — Add email on designation
15. `app/api/powers/route.ts` — Add auth check
16. `app/api/notifications/route.ts` — Add auth check
17. `app/api/departments/assign/route.ts` — Add auth check
18. `app/api/audit/route.ts` — Add auth check
19. `.env.example` — Add email function env vars
20. `proxy.ts` — Can be removed (replaced by middleware.ts)

## Verification
1. `npx tsc --noEmit` — type check passes
2. `npm run build` — build succeeds
3. Manual test: create event → submit for review → admin approves → publish
4. Manual test: register for event → ticket created → email sent
5. Manual test: dashboard shows real stats
6. Manual test: profile shows departments, designations
7. Manual test: unauthenticated user blocked from /admin
8. Manual test: non-admin blocked from admin pages
