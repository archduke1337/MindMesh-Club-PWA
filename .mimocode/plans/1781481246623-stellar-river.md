# Plan: Fix All Critical & Medium Issues

## Overview

Fix 11 issues identified in the gap analysis. Grouped by dependency for parallel execution.

---

## Fix 1: Wire proxy.ts properly

**Problem**: Next.js 16 uses `proxy.ts` but need to verify the export name matches what Next.js expects.

**File**: `proxy.ts`
- Ensure the exported function is named `proxy` (it already is)
- Verify the `config.matcher` includes all protected routes
- Add `/dashboard`, `/onboarding`, `/profile`, `/settings`, `/notifications` to matcher (currently only has `/admin/:path*`)

---

## Fix 2: Wire audit functions to all actions

**Problem**: `lib/audit.ts` defines 12 functions but none are called. API routes write audit entries directly.

**Files to modify**:
- `app/api/membership/route.ts` — call `logMembershipAction()` on approve/reject
- `app/api/promotions/route.ts` — call `logAudit()` on promotion
- `app/api/designations/route.ts` — call `logDesignationAction()` on assign/revoke
- `app/api/powers/route.ts` — call `logPowerAction()` on grant/revoke
- `app/api/departments/assign/route.ts` — call `logDepartmentAction()` on assign
- `lib/events.ts` `register()` — call `logRegistrationAction()` on register
- `lib/events.ts` `cancelRegistration()` — call `logRegistrationAction()` on cancel
- `app/profile/page.tsx` — call `logProfileChange()` on profile edits
- `app/admin/blog/page.tsx` — call `logBlogAction()` on approve/reject/delete
- `app/admin/gallery/page.tsx` — call `logGalleryAction()` on approve/reject/delete

**Approach**: Replace direct `databases.createDocument(COLLECTIONS.AUDIT_LOGS, ...)` calls with the audit service functions. For places that don't have audit calls yet, add them.

---

## Fix 3: Fix event type seeding

**Problem**: Seed has wrong types (Meeting, Social Event, Other) instead of spec types (Bootcamp, Meetup, Guest Lecture, Certification Exam).

**File**: `lib/seed.ts` — rewrite `seedEventTypes()` with correct 8 types from EVENT_TYPES.md:
1. Workshop — prerequisites, toolsNeeded, difficultyLevel, durationHours, handsOn, certificatesProvided
2. Hackathon — teamSizeMin, teamSizeMax, tracks, judgingCriteria, submissionRules, durationHours
3. Seminar — speakers, topicArea, certificateEligible, recordingAllowed, qnaAllowed
4. Competition — scoringRubric, submissionFormat, rounds, teamCompetition
5. Bootcamp — durationWeeks, curriculum, homeworkRequired, certificationProvided, difficultyLevel
6. Meetup — agenda, refreshments, networkingFocused
7. Guest Lecture — speakerBio, speakerCompany, topicArea, recordingAllowed, certificateEligible
8. Certification Exam — examBody, validityPeriod, retakePolicy, passingScore, examDuration

**File**: `app/admin/events/page.tsx` — fix hardcoded event type dropdown (line 560-572) to use actual seeded types instead of `conference, workshop, masterclass, competition, bootcamp, forum`.

---

## Fix 4: Create Lead Dashboard page

**File (new)**: `app/dashboard/lead/page.tsx`

Modules per DASHBOARDS.md §3:
- Department Overview card (team members count, events count)
- Event Pipeline (drafts pending approval)
- Team Management (core team list)
- Pending Approvals (registrations for exclusive events)
- Quick Actions (Create Event, Upload Resource)

---

## Fix 5: Create Head Dashboard page

**File (new)**: `app/dashboard/head/page.tsx`

Modules per DASHBOARDS.md §4:
- Operations Overview (multi-department stats)
- Event Approvals (events pending approval)
- Department Health cards (per-department metrics)
- Membership Queue (pending applications)
- Reports summary

---

## Fix 6: Update dashboard router for Lead/Head

**File**: `app/dashboard/page.tsx`
- For `lead` status: redirect to `/dashboard/lead` or render lead-specific content inline
- For `head` status: redirect to `/dashboard/head` or render head-specific content inline
- Simpler approach: add dedicated sections in the existing dashboard page for lead/head personas (like the existing admin section)

---

## Fix 7: Fix type mismatches

**File**: `app/api/designations/route.ts` line 56 — change `status: "active"` to `isActive: true`

**File**: `app/api/membership/route.ts` lines 70-77 — fix profile creation to match `Profile` interface:
- Use `userId` field properly
- Include all required fields from the Profile type

---

## Fix 8: Wire emails to event registration

**File**: `lib/events.ts` `register()` — after creating registration/ticket, call email:
```typescript
import { sendRegistrationEmail } from "./emailService";
// After registration succeeds:
await sendRegistrationEmail(userEmail, userName, { title, date, time, venue, location, organizerName, price, discountPrice });
```

**File**: `lib/emailService.ts` — add missing templates:
- `rejectionEmailTemplate(name, reason)` — for membership rejection
- `revocationEmailTemplate(name, role)` — for role/designation revocation
- `eventApprovalEmailTemplate(userName, eventTitle)` — for event approval
- `eventRejectionEmailTemplate(userName, eventTitle, reason)` — for event rejection
- `exclusiveRegistrationApprovedTemplate(userName, eventTitle)` — for exclusive registration approval
- `exclusiveRegistrationRejectedTemplate(userName, eventTitle, reason)` — for exclusive registration rejection
- `departmentAssignedTemplate(userName, departmentName)` — for department assignment

**Files to wire**:
- `app/api/membership/route.ts` — add rejection email
- `app/api/designations/route.ts` — add revocation email on DELETE
- `app/api/powers/route.ts` — add revocation email on DELETE
- `app/admin/events/page.tsx` — add approval/rejection emails
- `app/admin/events/[id]/registrations/page.tsx` — add approval/rejection emails
- `app/api/departments/assign/route.ts` — add department assigned email

---

## Fix 9: Fix admin dashboard hardcoded stats

**File**: `app/admin/page.tsx` — replace hardcoded 0 counts with real queries:
- Designations: query `DESIGNATIONS` collection, count documents
- Powers: query `POWERS` collection, count documents
- Audit Log: query `AUDIT_LOGS` collection, count documents

**File**: `app/admin/membership/page.tsx` lines 98-99 — replace hardcoded "Approved Today" / "Rejected Today" with real queries filtering by `reviewedAt` date.

---

## Fix 10: Add missing profile fields

**File**: `app/profile/page.tsx` — add display and editing for:
- URN
- Academic details (program, branch, year, semester)
- Address
- Date of birth
- Gender
- Pronouns
- Profile visibility setting

---

## Fix 11: Add department assignment flow for members

**File (new)**: `app/departments/page.tsx` — already exists as browse page. Enhance with:
- "Request to Join" button on each department card
- Department request form (select preference order: 1st, 2nd, 3rd choice)
- My Requests section showing pending requests

**File**: `lib/departments.ts` — add methods:
- `requestJoin(userId, departmentId, preference)` — create department request
- `getMyRequests(userId)` — get user's pending requests
- `approveRequest(requestId, approverId)` — approve and assign
- `rejectRequest(requestId, reason)` — reject with reason

**File (new)**: `lib/departmentRequests.ts` or add to `lib/departments.ts` — CRUD for department request records

---

## Files to Create (New)
1. `app/dashboard/lead/page.tsx` — Lead dashboard (or enhance existing)
2. `app/dashboard/head/page.tsx` — Head dashboard (or enhance existing)

## Files to Modify
1. `proxy.ts` — add more routes to matcher
2. `lib/audit.ts` — ensure all functions are properly exported
3. `app/api/membership/route.ts` — wire audit + rejection email
4. `app/api/promotions/route.ts` — wire audit
5. `app/api/designations/route.ts` — wire audit + fix type + revocation email
6. `app/api/powers/route.ts` — wire audit + revocation email
7. `app/api/departments/assign/route.ts` — wire audit + email
8. `lib/events.ts` — wire audit + email on registration
9. `app/profile/page.tsx` — wire audit + add missing fields
10. `app/admin/blog/page.tsx` — wire audit
11. `app/admin/gallery/page.tsx` — wire audit
12. `lib/seed.ts` — fix event types
13. `app/admin/events/page.tsx` — fix event type dropdown + emails
14. `app/admin/events/[id]/registrations/page.tsx` — add emails
15. `app/dashboard/page.tsx` — add lead/head sections
16. `app/admin/page.tsx` — fix hardcoded stats
17. `app/admin/membership/page.tsx` — fix hardcoded stats
18. `lib/emailService.ts` — add missing templates
19. `app/departments/page.tsx` — add request flow
20. `lib/departments.ts` — add request methods

## Verification
1. `npx tsc --noEmit` — zero type errors
2. `npm run build` — build succeeds
3. Audit log entries created on membership approval, promotion, designation, event registration
4. Emails sent on registration, approval, rejection (when EMAIL_WEBHOOK_URL is set)
5. Lead/Head dashboard shows role-specific content
6. Admin dashboard stats are real numbers
7. Profile shows all fields
8. Department request flow works end-to-end
